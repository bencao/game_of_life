defmodule Engine.JudgeTest do
  use ExUnit.Case, async: true

  test "tick should start processing when judge is idle" do
  end

  test "tick should impact nothing when judge is judging" do
  end
end
